# Model runtime now installs to ~/.maple-agent/runtime, outside the project.
# Still ignored here in case someone points MAPLE_DIR back inside.
runtime/

# Build output
dist/
*.tsbuildinfo

# Dependencies
node_modules/

# Secrets — never commit the API key
token
*.pem
.env
.env.*

# Local data
*.db
*.db-wal
*.db-shm
*.log

# OS
.DS_Store

# Inspector UI build output
ui/dist/
__pycache__/
