# Model runtime: an upstream clone plus ~5GB of weights.
# Recreated by install.sh; must never enter this history.
runtime/

# Build output
dist/
*.tsbuildinfo

# Dependencies
node_modules/

# Secrets — never commit the API key
token
*.pem
.env
.env.*

# Local data
*.db
*.db-wal
*.db-shm
*.log

# OS
.DS_Store
