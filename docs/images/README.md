Screenshots of the desktop app.

Kept few and deliberately: every UI change invalidates one, and a stale
screenshot is worse than none because it is believed. Only add one where the
interface *is* the subject — the approval sheet, the recipes drawer, the model
picker — never to decorate prose that already says it.
